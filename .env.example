# MetaTube CSV Server 环境变量配置
# 复制此文件为 .env 并根据需要修改

# CSV 数据文件路径
CSV_PATH=/data/BB_Magnet.csv

# SQLite 数据库路径（可选，默认与 CSV 同名，如 /data/BB_Magnet.db）
# DB_PATH=/data/movies.db

# 真实 MetaTube Server 地址，本地查不到时回退；留空则不回退
FALLBACK_URL=https://metatube-server.hf.space
